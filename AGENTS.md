# AGENTS.md — textro99-server

このファイルは、AIコーディングエージェント（Claude Code / Codex / Cursor 等）が **textro99-server**（Go）で作業する際に毎セッション最初に読む憲法。ツールに依存しない共通ルールとして、ここに一本化する。
ここに書かれた責務・依存ルール・禁止事項は、他のどの指示よりも優先する。

> Claude Code は `CLAUDE.md` からこのファイルを参照している。どのAIツールを使っても、実体はこの `AGENTS.md` を読むこと。

---

## 0. このリポジトリは何か

99人バトルロイヤル型タイピングゲーム「テキストロ99」（仮称）の**ゲームサーバー（Go）**。
戦闘の権威（コンボ・威力・相殺・脱落・KO・ターゲティング）をすべて確定させる、対戦の真実を握るサーバー。

**マルチレポの一部**。他リポジトリ: `textro99-proto`（契約・唯一の結合点）、`textro99-web`（テストフロント）、`textro99-unity`（本番クライアント）。このリポジトリが依存してよいのは `textro99-proto` だけ。

> ⚠️ Go モジュール名は `textro99`。プロダクト名確定前の暫定なので、変えるならチーム合意の上で `go.mod` とインポートパスを一括置換する。勝手にリネームしない。
> ※ 設計ドキュメント（別リポジトリ Textro99-Docs）には旧称 `sushida99` 表記が残っている箇所があるが、コードでは `textro99` を使う。

---

## 1. 最重要ルール（違反厳禁）

### 1.1 戦闘の権威はサーバー。打鍵判定はやらない
- コンボ・威力・相殺・ダケンスタック・脱落・KO・難易度・ターゲット解決は**すべてこのサーバーが確定**する。
- **打鍵の正誤判定（TypingJudge）はクライアント側の責務**。このサーバーには実装しない。サーバーは `DakenClearReport`（判定済みの結果）を受け取り、`dakenId`・`elapsedMs` の整合性を検証するだけ。キー列は受け取らない。

### 1.2 proto は契約。勝手に変えない
- メッセージ定義・DTO は `textro99-proto` が正典。このサーバーは proto の型に従って送受信する。
- **proto を変更する必要が出たら、実装を進める前に必ず一旦止めて、変更案を人間（りーせ）に提示して承認を得る**。承認なしに proto を変えて突き進まない。proto の変更は web/unity にも波及する契約変更なので、勝手に確定させない。

### 1.3 調整値をハードコードしない
- コンボ係数・威力係数・予告猶予・スタック上限・マッチング人数などの数値は、**すべて `GameParameters` 経由**で外部（リモートコンフィグ）から起動時取得する。
- コードに数値を直書きしない。取得失敗時はサーバー内蔵のデフォルト値で起動する（オフライン開発を止めない）。

### 1.4 層アーキの依存を守る
- **コア game**（層1）は per-player 判定式（コンボ/威力/相殺/スタック/難易度）と session 状態機械（全プレイヤー横断の調停：ターゲティング/予告ライフサイクル/KO/生存人数）を内包する、純粋・Tick(dt)駆動のコア。
- コア game は**部品(targeting/odai/config/bot/store)・スパイン(room/matchmaking/transport)を import しない**（proto は可）。作戦/お題/設定は `game/ports.go` の interface を注入して呼ぶ（DIP）。
- 依存は常に **部品/スパイン → game の一方向**。詳細は 3章と `docs/architecture.md`。

---

## 2. パッケージ構成（提案・Claude Codeはこれを基準に、逸脱時は理由を述べる）

> 層アーキ（層1コア / 層2 interface / 層3部品 ＋ スパイン ＋ 契約）を採用。詳細は `docs/architecture.md`。

```
cmd/
  server/
    main.go            … 合成ルート。全部品を配線。--mode solo / --mode match を切替

internal/
  game/                … 【層1・コア】戦闘の権威。純粋・Tick(dt)駆動。他の internal 部品/スパインを import しない（proto は可）
    session.go         …   1試合の状態機械。Tick(dt)で進む。全員のスタック/予告/KO/生存人数を調停
    combo.go           …   コンボ蓄積/減衰/消費（per-player 判定式）
    attack.go          …   威力算出・威力→ダケン個数変換
    offset.go          …   相殺差分・撃ち返し連鎖（上限あり）
    stack.go           …   ダケンスタック・トラップ誘発（ハイウォーターマーク）・脱落判定
    difficulty.go      …   経過時間×コンボ→難易度
    params.go          …   GameParameters（受け皿）
    ports.go           …   【層2・継ぎ目】コアが要求する interface（DIP）: TargetingStrategy / WordSource / ConfigProvider

  targeting/           … 【層3部品】作戦0〜9。game.TargetingStrategy を実装。1ファイル1作戦（追加式）
  odai/                … 【層3部品】お題供給。game.WordSource を実装
  config/              … 【層3部品】GameParameters 取得（リモートコンフィグ）。game.ConfigProvider を実装
  bot/                 … 【層3部品】CPUの自動入力（DakenClearReport相当を内部生成）
  store/               … 【層3部品】永続化の口（現状 Noop。ランキング等が生えたら実装）

  room/                … 【スパイン】tickループ駆動。goroutine＋実ticker＋channel で session.Tick を回し入力適用・配信
  matchmaking/         … 【スパイン】待機プール・人数下限＋カウントダウン・Bot補完
  transport/           … 【スパイン】coder/websocket・状態配信（間引き）・接続ライフサイクル
    connection.go      …   Connection interface（実WS / InMemory を切替）
    publisher.go       …   PlayerListUpdated の間引き配信
  proto/               … 【契約】canonical textro99-proto を参照する薄い再輸出ラッパ
```

- `internal/` に置くのは Go の作法で、モジュール外から import されなくするため。
- パッケージ名・分割は Claude Code が最適化してよいが、**1.4 の依存方向と、game が部品/スパインを import しない原則は崩さない**。
- 「スパイン」は層に属さない特別枠（コアをネットワークに繋いで試合を回す統合部）。りーせが持つ。
- ⚠ `match` という語は使わない（試合セッション=game内 / マッチング=matchmaking と紛れるため）。

---

## 3. コア game 内の実装ガイド（重要・過剰設計を避ける）

> コア game は「per-player 判定式（純関数）」と「session（全プレイヤー横断の調停）」を内包する。両者はいずれも `game` パッケージ内で、パッケージ境界ではなくファイル/責務の分担。

「判定式は純関数、横断状態は session が持つ」という設計方針だが、**純度を上げすぎて過剰に複雑にしないこと**。

- **やること**: コンボ計算・威力計算・相殺差分・スタック増減判定といった**判定式そのもの**を、副作用のない関数として書く。同じ入力から同じ出力が出る形にし、Goでヘッドレスにtick駆動シミュレートできるようにする。
- **やりすぎないこと**: 「1人に明らかに属する状態（そのプレイヤーのコンボ値・スタックの中身）」まで全部 session に外出しして、判定式に毎回全状態を引数で渡す、という極端な純化はしない。per-player の状態は per-player の構造体がまとめて持ってよい。
- **線引き**: 純関数に保つのは「**判定式（計算ロジック）**」。session が持つのは「**全プレイヤー横断の状態と時系列**」（誰が誰を予告中か、KOは誰の手柄か、生存人数、順位）。1人分の状態は per-player 構造体が持つ。
- 迷ったら「この状態は1人に属するか、複数人にまたがるか」で判断する。1人なら per-player、複数人なら session。

---

## 4. 実装時に特に注意する3点（設計レビューで挙がった論点）

### 4.1 99人負荷検証は「ヘッドレスシミュレーション」とは別物
- ドキュメントにある「Goでヘッドレスに1試合を数秒でシミュレート」は**バランス調整用**（数値が5〜10分に収まるか）で、メモリ内でロジックだけ回す。
- これとは別に、**99人 × WebSocket接続 × 状態配信をサーバーが実際に捌けるかの負荷検証**が必要（企画の核＝99人対戦の成立に直結）。
- 実現方法: `transport` に **InMemory Connection** を用意し、Bot 99体を実際の配信ループに乗せて回す。実クライアント（Web/Unity）の完成を待たずにネットワーク層・配信負荷を検証できる。
- これは専用issueとして早期に立てる（ドキュメントでも「99人同期のリアルタイム性検証」は未着手と自認されている項目）。

### 4.2 Bot の入力は「実在する dakenId」と整合させる
- Bot はサーバー内の自動入力クライアント。`DakenClearReport` 相当を内部生成するが、**チート対策（`dakenId` 検証）と衝突しないよう、サーバーが発行した実在の dakenId に対してクリア報告を返す**必要がある。
- Bot も「サーバーが dakenId を発行 → Bot がその id に対してクリア報告」という、人間と同じ発行・報告のサイクルに乗せる。dakenId を無視した report を捏造しない。
- 設計の綺麗さのため、Bot も人間も同じ `Connection` interface（InMemory実装）に乗せ、session から見て区別しない形が望ましい。

### 4.3 PlayerListUpdated は間引く（全員に全員分フルは禁止）
- 99人分の PlayerSummary を99人へ毎tick配信すると O(99×99) で帯域が破綻する。
- `transport/publisher.go` で間引く: 送信頻度を下げる（数Hz）、変化したプレイヤーのみ差分送信、ペイロードを表示に要る最小フィールドに絞る、スタック量を量子化する等。
- 初期は素朴なフル配信で動かしてよいが、**間引きは差し替え可能な形**にしておき、負荷が見えてから強化する（proto のメッセージ形に影響し得るので、方式を決めてから proto を確定）。

---

## 5. 技術スタック（確定・変更しない）

| 項目 | 選定 |
|------|------|
| 言語 | Go |
| WebSocket | coder/websocket（`github.com/coder/websocket`） |
| ホスティング | Render（Web Service。本番前に Starter 以上でスリープ無効化） |
| 調整パラメータ | リモートコンフィグ（GAS+スプレッドシート推奨）。GameParameters 経由で取得 |
| 試合中の状態 | メモリ（Goの構造体）。DB/Redis は使わない |
| 永続化 | 現状なし（store.ResultStore の Noop のみ。ランキング等が生えたら実装） |

- 試合中の状態を DB/Redis に置かない（tick毎更新のためメモリ）。
- スケールアウト（複数インスタンス）は現状不要。99人1部屋は1台で捌ける前提。必要になってから Redis 等を検討。

---

## 6. コマンド

```bash
go build ./...
go test ./...
go test -race ./internal/game/...     # コア game のテスト（落ちたらマージ不可）
go vet ./...
golangci-lint run                      # depguard で依存ルールを機械検査

go run ./cmd/server --mode match       # 本番同等（マッチングプール起動）
go run ./cmd/server --mode solo        # ローカル1人（マッチング迂回）
```

- `go test ./internal/game/...` が常時グリーンであることがコアの安全保証。
- 依存ルール（game が部品/スパインを import しない等）は `.golangci.yml` の depguard で機械強制済み。CI で自動的に弾かれる。

---

## 7. 作業の進め方

- **1 issue = 1 PR = 1つの完結タスク**。PR は担当パッケージのフォルダ内に閉じる。
- **proto / 契約に触る作業は、実装前に変更案を人間に提示**（1.2）。
- タスクの詳細仕様は Textro99-Docs（設計ドキュメント）の `03_サーバー仕様/` 各ファイルを参照。実装前に該当ドキュメントを読む。

### セッション開始時の手順
1. この CLAUDE.md を読む。
2. 設計ドキュメントの `03_サーバー仕様/01_試合進行仕様.md`（責務分界・シーケンス）と、触る機能の該当仕様を読む。
3. これから触るパッケージの依存ルール（1.4・3章）を確認する。
4. proto に触るなら、実装前に変更案を人間に提示する（1.2）。

---

## 8. やってはいけないことチェックリスト

- ❌ 打鍵判定（TypingJudge / ローマ字解決）をサーバーに実装する（クライアントの責務）
- ❌ proto を人間承認なしに変更して実装を進める
- ❌ `internal/game/` から部品/スパインの internal パッケージを import する（依存は逆向き＝部品/スパイン→game）
- ❌ 調整値をコードに直書きする（GameParameters 経由必須）
- ❌ 試合中の状態を DB/Redis に置く（メモリで持つ）
- ❌ PlayerListUpdated を全員へ全員分フル配信する（間引く）
- ❌ 撃ち返し連鎖に上限を設けない（`attack.maxReboundChain` で必ず止める）
- ❌ 純関数化を過剰にやって、1人分の状態まで session に外出しする（3章）
- ❌ 技術スタック（5章）を勝手に変える
- ❌ プロダクト名・モジュール名を勝手にリネームする（`textro99` を維持）
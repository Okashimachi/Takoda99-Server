# AGENTS.md — Takoda99-Server

このファイルは、AIコーディングエージェント（Claude Code / Codex / Cursor 等）が **Takoda99-Server**（Go）で作業する際に毎セッション最初に読む憲法。ツールに依存しない共通ルールとして、ここに一本化する。
ここに書かれた責務・依存ルール・禁止事項は、他のどの指示よりも優先する。

> Claude Code は `CLAUDE.md` からこのファイルを参照している。どのAIツールを使っても、実体はこの `AGENTS.md` を読むこと。

---

## 0. このリポジトリは何か

99店バトルロイヤル型のたこ焼き経営タイピングゲーム「**タコダ99**」の**ゲームサーバー（Go）**。
99 のたこ焼き屋が客を取り合い、120秒でスコアを競う。試合の権威（客の分配・スコア・脱落・順位）をすべて確定させるサーバー。

### ゲームの骨子（これを外すと全部ズレる）

- **直接攻撃はない**。他店を殴る手段は存在しない。競争は「客の奪い合い」を通じた**間接的なもの**。
- **客は逃げない**。我慢ゲージも離脱も無い。一度出したお題は必ず打ち切られる。
- 順位を決めるのは**スコア**ただ1つ。`W_TAKOYAKI×たこ焼き数 − W_MISS×ミス数` の累計（**絶対値**）。
  **0 でクランプしない**（負値を許容する）。
- **20秒等間隔の時刻スケジュール（cullSchedule）**でスコア下位から強制脱落する
  （99→75→55→35→20→10→0）。**120秒の最終ステージで全店が同時に脱落して試合が終わる**。
- 客は**行列が短い店ほど来やすい**。それだけ。**スコアで客の来やすさを変えない**
  （高スコア→客増→さらに伸びる、の正のフィードバックが二重にかかり決勝の逆転劇が死ぬ）。
- **最終順位は脱落順で決まる**。同一ステージで同時に落ちた分のタイブレークは
  スコア→正確性→速度→storeId（`weakerForRank` が順位付けと足切りで共有する唯一の基準）。

> ⚠ 旧プロジェクト「テキストロ99（Textro99）」からの企画転換で生まれたリポジトリ。
> **直接攻撃・コンボ・威力・相殺・ダケンスタック・KO・ターゲティング（作戦）は全て廃止済み**。
> 古いコード・ドキュメント・issue にこれらの語が残っていたら、それは消し忘れであって仕様ではない。復活させない。
>
> ⚠ さらに 2026-08-12 の企画転換で、**信用（ライフ）・自滅脱落（SelfCollapse）・客の我慢ゲージと離脱・
> 客属性による評価の増減・評価EMA・パーセンタイル正規化・星評価・storm（tick周期の下位%淘汰）も
> 全て廃止済み**。これらも同様に、残っていたら消し忘れであって仕様ではない。復活させない。

### マルチレポ構成

| リポジトリ | 役割 |
|---|---|
| **Takoda99-Proto** | 契約の正典。メッセージ型・列挙・DTO。**唯一の結合点** |
| **Takoda99-Docs** | 設計の正典。企画書・用語集・全体仕様・サーバー仕様 |
| **Takoda99-Server**（本体） | ゲームサーバー（Go） |
| **Takoda99-Unity** | **クライアント（C#/Unity）。唯一のクライアント** |
| **Takoda99-Client-Docs** | クライアント実装者向けドキュメント |
| ~~Takoda99-WebFront~~ | ~~Web クライアント（TS）~~ **廃止・凍結**（下記） |

このリポジトリが依存してよいのは **Takoda99-Proto だけ**。

> ⚠ **Takoda99-WebFront は廃止された**。「Web先行 → Unity流用」戦略をやめ、
> **最初から Unity 単独で開発する**方針に変更されている（`Takoda99-WebFront/AGENTS.md` 冒頭に明記）。
> リポジトリは実装されないまま凍結されており、**クライアントは `Takoda99-Unity` だけ**。
> plan/docs に「WebFront と結合する」という記述が残っていたら、それは消し忘れであって手順ではない。

> Go モジュール名は `takoda99`。勝手にリネームしない。

---

## 1. 最重要ルール（違反厳禁）

### 1.1 試合の権威はサーバー。打鍵判定はやらない

サーバーが確定させるもの:

- 客の生成・**分配**・再分配
- **スコア**の算出・累積と**順位**の確定
- **お題単語の発行**
- **時刻足切り(cullSchedule)・フェーズ・火力**の管理と脱落の確定
- 試合進行（開始・終了・順位確定）

クライアントの責務は**打鍵の正誤判定とその集計だけ**。サーバーは `OrderServed`（注文N個を打ち切った報告：`customerId` / `elapsedMs` / `missCount`）を受け取り、サニティ検証してからスコアに反映する。**キー列は受け取らない**。
（`elapsedMs` はスコアには使わない。サニティ検証と統計にのみ使う。）

**試合中の C2S は実質 `OrderServed` のみ**。完成報告（1個ごと）・離脱報告・脱落報告は存在しない — すべてサーバーが自律的に確定する。クライアントから「客が離脱しました」「脱落しました」を送らせない。

### 1.2 proto は契約。勝手に変えない

- メッセージ定義・DTO は **Takoda99-Proto が正典**。このサーバーは proto の型に従って送受信する。
- **proto を変更する必要が出たら、実装を進める前に必ず一旦止めて、変更案を人間（りーせ）に提示して承認を得る**。承認なしに proto を変えて突き進まない。proto の変更は Unity クライアントにも波及する契約変更。
- `internal/proto/messages.go` は canonical の薄い再輸出ラッパ。ここに独自の型を生やさない。

### 1.3 調整値をハードコードしない

- スコアの重み（`score.weightTakoyaki` / `score.weightMiss`）・足切りスケジュール（`cull.stages`）・フェーズ閾値・火力係数・補充閾値・マッチング人数などの数値は、**すべて `GameParameters` 経由**で外部（config）から取得する。
- コードに数値を直書きしない。取得失敗時はサーバー内蔵のデフォルト値で起動する（オフライン開発を止めない）。
- **`GameParameters` の全フィールドは `==` 比較可能（comparable）に保つ**。`map` や slice をフィールドに入れない（config の差分検出・backfill が壊れる）。属性別の値は固定フィールドの struct で持つ。

### 1.4 層アーキの依存を守る

- **コア game**（層1）は 1試合の状態機械。**純粋・Tick(dt)駆動**（時計・ネットワーク・I/O・ログを持たない）。
- コア game は**部品(odai/config/bot/store)・スパイン(room/matchmaking/transport)を import しない**（proto は可）。お題/設定は `game/ports.go` の interface を注入して呼ぶ（DIP）。
- 依存は常に **部品/スパイン → game の一方向**。`.golangci.yml` の depguard で機械強制済み。
- game は結果を `[]Outbound`（宛先つきメッセージ）で返すだけ。実際の配信は room が行う。

---

## 2. パッケージ構成

> 層アーキ（層1コア / 層2 interface / 層3部品 ＋ スパイン ＋ 契約）。詳細は `docs/architecture.md`。

```
cmd/
  server/
    main.go            … 合成ルート。全部品を配線。--mode solo / --mode match を切替

internal/
  game/                … 【層1・コア】試合の権威。純粋・Tick(dt)駆動。他の internal 部品/スパインを import しない（proto は可）
    session.go         …   1試合の状態機械。客レジストリ/行列/たべたべエリア/店舗状態と tick ループ
    params.go          …   GameParameters（全調整値）
    ports.go           …   【層2・継ぎ目】コアが要求する interface（DIP）: WordSource / ConfigProvider

  odai/                … 【層3部品】お題単語供給。game.WordSource を実装
  config/              … 【層3部品】GameParameters 取得。game.ConfigProvider を実装
  db/                  … 【層3部品】Postgres（設定・試合結果）
  bot/                 … 【層3部品】CPUの自動入力（OrderServed を内部生成）
  store/               … 【層3部品】試合結果永続化の口

  sim/                 … 【開発ツール】game.Session を直接 tick 駆動するヘッドレスシミュレータ
                       …   バランス調整(cmd/matchsim)と決着保証テストが共有。sim → game の一方向

  room/                … 【スパイン】tickループ駆動。session.Tick を回し入力適用・配信
  matchmaking/         … 【スパイン】待機プール・人数下限＋カウントダウン・Bot補完
  transport/           … 【スパイン】coder/websocket・状態配信（間引き）・接続ライフサイクル
  configapi/           … 【スパイン】config-front 用 REST API（/api/params）
  admin/               … 【スパイン】観測配信。AdminHub（読み取り専用ファンアウト）＋ /admin 静的同梱。
                       …   room が publish() で Broadcast。game を import しない（admin→transport の一方向）
  proto/               … 【契約】canonical Takoda99-Proto を参照する薄い再輸出ラッパ
```

- `internal/` に置くのは Go の作法で、モジュール外から import されなくするため。
- パッケージ名・分割は最適化してよいが、**1.4 の依存方向と、game が部品/スパインを import しない原則は崩さない**。
- 「スパイン」は層に属さない特別枠（コアをネットワークに繋いで試合を回す統合部）。りーせが持つ。
- ⚠ `match` という語を単独で使わない（試合セッション=game内 / マッチング=matchmaking と紛れるため）。

### tick ループの順序（変えない）

```
1. stepPhase      … フェーズ判定（Early/Mid/Late）
2. stepDistribute … 客分配（restPool→行列）。重みは行列の短さのみ
3. stepRank       … 生存店をスコア降順に並べて rank → EvaluationUpdate
4. stepHeat       … 火力（お題難度）更新
5. stepCull       … 時刻足切りの実行・予告（常時）
6. checkFinish    … 最終ステージ到達で終了・順位確定・MatchEnd
```

順序には意味がある。`stepPhase` が先なのは分配の Claimer 解禁判定に要るため、`stepCull` が `stepRank` の後なのは足切り対象の選定に**その tick のスコア順位**が要るため。

**スコアは `ApplyOrderServed` で加算される**ので、tick 側にスコアを進める処理は無い（`stepRank` は並べるだけ）。

---

## 3. コア game 内の実装ガイド（過剰設計を避ける）

「判定式は純関数、横断状態は session が持つ」という方針だが、**純度を上げすぎて過剰に複雑にしないこと**。

- **やること**: スコア加算・順位のタイブレーク判定・火力計算といった**判定式そのもの**を、副作用のない形で書く。同じ入力から同じ出力が出るようにし、ヘッドレスに tick 駆動シミュレートできるようにする。
- **やりすぎないこと**: 1店に明らかに属する状態（その店のスコア・行列・提供集計）まで全部引数で渡し回す極端な純化はしない。per-store の状態は `storeState` がまとめて持ってよい。
- **線引き**: 純関数に保つのは「**判定式（計算ロジック）**」。session が持つのは「**全店横断の状態と時系列**」（客が今どこにいるか、生存店数、フェーズ、順位）。
- 迷ったら「この状態は1店に属するか、複数店にまたがるか」で判断する。

### 客データの扱い（重要）

客の実体は `customers` レジストリが**単一の情報源**として持つ。移動は **ID 配列の増減のみ**で表す（実体を複製・破棄しない）。

- `storeQueues[storeId]` … 各店の行列。**先頭 = 対応中**
- `restPool` … たべたべエリア（未割当）
- `customer.assignedStore` … 割り当て先（nil = 未割当）

この3つが食い違うと客が消える/増える。移動は必ず `assignCustomer` / `releaseToRest` ヘルパ経由で行い、直接書き換えない。

---

## 4. 実装時に特に注意する点

### 4.1 99店負荷検証は「ヘッドレスシミュレーション」とは別物

- 「ヘッドレスに1試合を数秒でシミュレート」は**バランス調整用**（生存99→1の所要時間が想定に収まるか）。メモリ内でロジックだけ回す。
- これとは別に、**99接続 × WebSocket × 状態配信を実際に捌けるかの負荷検証**が必要。
- 実現方法: `transport` の **InMemory Connection**（`Pipe()`）で Bot 99体を実際の配信ループに乗せる。実クライアントの完成を待たずに検証できる。

### 4.2 Bot の入力は「実在する customerId」と整合させる

- Bot はサーバー内の自動入力クライアント。`OrderServed` を内部生成するが、**サーバーが `CustomerArrived` で来店させた実在の客に対して報告する**。
- 存在しない `customerId` や、行列先頭でない客に対する報告を捏造しない（サーバー側のサニティ検証で弾かれる）。
- Bot も人間も同じ `Connection` interface（InMemory 実装）に乗せ、session から見て区別しない。

### 4.3 StoreListUpdate は間引く（全員に全員分フルは禁止）

- 99店分の `StoreSummary` を99店へ毎tick配信すると O(99×99) で帯域が破綻する。
- `transport/publisher.go` で間引く: 送信頻度を下げる（数Hz）、変化した店のみ差分送信、ペイロードを表示に要る最小フィールドに絞る。
- 初期は素朴なフル配信で動かしてよいが、**間引きは差し替え可能な形**にしておく。

### 4.4 属性でゲーム挙動を分岐しない

客属性（Normal / Bonus / Claimer / Buzz）は**見た目の出し分け専用**。ゲーム挙動を属性で分岐させない。特にスコアの重みを属性ごとに変えない。変えてよいのは出現率(`weight`)と注文数(`orderCount`)だけで、**同じ打鍵をすれば属性によらず同じスコアになる**こと。

- **本戦での理由**: 属性がスコアに影響すると「同じように打ったのに評価が違う」が戻り、本戦が解こうとした納得感の欠如そのものを再導入することになる（企画書 3.2）。
- **過去の事例（#29）**: 予選では離脱判定を属性で分岐した結果、特定属性が離脱せず行列先頭に居座って店が永久に詰まった。**属性分岐は過去にも壊している。**

壊れ方は違う（機能不全 / 納得感の破壊）が、**属性で分岐すると一部属性だけ壊れる**という失敗の型は同じ。

---

## 5. 技術スタック（確定・変更しない）

| 項目 | 選定 |
|------|------|
| 言語 | Go |
| WebSocket | coder/websocket（`github.com/coder/websocket`） |
| ホスティング | **GCP Compute Engine（e2-micro）**。systemd 常駐 ＋ Caddy で TLS 終端。`docs/deploy.md` |
| 調整パラメータ | Postgres + config-front（Next.js / Vercel）。`GameParameters` 経由で取得 |
| **ライブ試合状態** | **メモリ（Goの構造体）**。DB/Redis に置かない |
| 永続化 | **Postgres をゲームサーバーが所有**。設定・試合結果・BOT調整用データ |

- **ライブ試合状態（tick 毎に変わるもの）は必ずメモリ**。ここを DB/Redis に置かない。
- **それ以外（設定・試合結果・分析用データ）は Postgres**。「NO-DB」ではない。
- config の反映タイミング: **試合系パラメータは次の試合から**（再起動不要）。**matching 系も再起動不要**で、
  `GetParams` が待機ループのたびに `provider.Load` を呼ぶため数秒で反映される（当日 `minPlayers` を下げる運用の前提）。
- スケールアウト（複数インスタンス）は現状不要。99店1部屋は1台で捌ける前提。

---

## 6. コマンド

```bash
go build ./...
go test ./...
go test -race ./internal/game/...     # コア game のテスト（落ちたらマージ不可）
go vet ./...
golangci-lint run                      # depguard で依存ルールを機械検査

go run ./cmd/server --mode match       # 本番同等（マッチングプール起動）
go run ./cmd/server --mode solo        # ローカル1人＋Bot（マッチング迂回）

go run ./cmd/matchsim                             # ヘッドレス1試合（バランス調整用・99店）
go run ./cmd/matchsim --runs 20 --quiet           # 20回まわして決着時間の統計
go run ./cmd/matchsim --profile uniform --seed 42 # 実力分布を変える／結果を再現する

go test ./internal/sim/                              # 決着保証テスト（CI 常設）
go test -v ./internal/sim/ -run ReportTiming         # 決着時間の実測表
```

- `cmd/matchsim` は **バランス調整**の道具（メモリ内・大きい dt で高速）。
  99接続を捌けるかの**性能検証**は別物で、そちらは Plan-18 の負荷テストが担う（4.1）。

- `go test ./internal/game/...` が常時グリーンであることがコアの安全保証。
- 依存ルール（game が部品/スパインを import しない等）は depguard で機械強制済み。CI で自動的に弾かれる。

---

## 7. 作業の進め方

- **1 issue = 1 PR = 1つの完結タスク**。PR は担当パッケージのフォルダ内に閉じる。
- **クライアントから見える挙動を変えたら `docs/client-integration.md` も同じ PR で直す。**
  ワイヤ仕様とタイミング（どの型がいつどの宛先へ届くか）の正典はこのファイル。
  型そのものは Takoda99-Proto、クライアント側の設計は Takoda99-Client-Docs が担う。
- **main へ直接 push しない**。必ずブランチを切って PR を出す。
- **proto / 契約に触る作業は、実装前に変更案を人間に提示**（1.2）。
- タスクの詳細仕様は Takoda99-Docs の `03_サーバー仕様/` 各ファイルを参照。

### issue と plan の役割分担

| | 役割 | 正典なのは |
|---|---|---|
| **GitHub issue** | 作業の**追跡と議論**。オープン/クローズで進捗が見える | 「今どこまで進んだか」 |
| **`docs/plan/`** | **仕様書**。何をどう作るかの詳細 | **内容** |

- 着手前に該当 plan を読む。plan 同士で型・関数シグネチャが噛み合うよう調整済みなので、
  **勝手に別案に差し替えない**。
- **内容が食い違ったら plan が正しい**。issue 本文は起票時点のスナップショットで古くなりやすい。
- issue 本文が**誤り**になった場合（前提が覆った等）は、放置せず
  「訂正内容＋正しい方法」を書いてクローズし、plan へ一本化する。
  誤った issue を開いたまま残すと、それを読んだ人が誤った作業をする。
- plan は書き換えるより**取り消し線で誤りを残して訂正を併記**する方がよい。
  「なぜ変わったか」が追えなくなると、同じ誤りが別の文書へ再び写される。

> 実例: `plan-10` §1.2 の「matching 系は再起動が必要」という誤記が `docs/deploy.md` へ、
> さらに issue #36 へと伝播した。発生源を直さないまま下流だけ直しても再発する。

### セッション開始時の手順

1. この `AGENTS.md` を読む。
2. `docs/architecture.md`（層アーキの具体）を読む。
3. これから触る機能の `docs/plan/plan-NN_*.md` を読む（一覧は `docs/plan/README.md`）。
4. 必要なら Takoda99-Docs の `03_サーバー仕様/01_試合進行仕様.md`（責務分界・シーケンス）を読む。
5. proto に触るなら、実装前に変更案を人間に提示する（1.2）。

### 設計文書と実装が意図的に食い違う箇所

Docs が常に正しいわけではない。**承認済みの逸脱**は以下。Docs を根拠に巻き戻さないこと。

| 箇所 | Docs | 実装 | 理由 |
|---|---|---|---|
| ~~客分配の重み~~ | ~~`正規化評価 ÷ (行列長+1)`~~ | ~~`(WeightFloor + 正規化評価) ÷ (行列長+1)`~~ | **役目終了（h21）**。評価そのものが廃止され、重みは `1 / (行列長+1)` になった。`WeightFloor` も削除済み |

現在この表に生きている逸脱は無い（記録は消さず取り消し線で残す。「なぜ変わったか」が追えなくなると同じ誤りが再び写される）。

---

## 8. やってはいけないことチェックリスト

- ❌ 打鍵判定（TypingJudge / ローマ字解決）をサーバーに実装する（クライアントの責務）
- ❌ proto を人間承認なしに変更して実装を進める
- ❌ `internal/game/` から部品/スパインの internal パッケージを import する（依存は逆向き）
- ❌ `internal/game/` にログ・時計・ネットワーク・DB を持ち込む（純粋コアを汚さない）
- ❌ 調整値をコードに直書きする（GameParameters 経由必須）
- ❌ `GameParameters` に map / slice のフィールドを足す（`==` 比較が壊れる）
- ❌ **ライブ試合状態**を DB/Redis に置く（メモリで持つ。設定・結果の永続化は別で Postgres を使う）
- ❌ StoreListUpdate を全員へ全員分フル配信する（間引く）
- ❌ 客属性ごとにスコア・分配・足切りの挙動を分岐する（4.4・納得感の破壊／#29 の詰まり）
- ❌ 客の実体を複製する／`storeQueues`・`restPool`・`assignedStore` を直接書き換える（ヘルパ経由）
- ❌ 直接攻撃・コンボ・威力・相殺・ダケンスタック・KO・ターゲティング（作戦）を実装・復活させる（**廃止済み**）
- ❌ 信用（ライフ）・自滅脱落・客の我慢ゲージと離脱・評価EMA・パーセンタイル正規化・星評価・storm（tick周期の下位%淘汰）を実装・復活させる（**廃止済み**）
- ❌ スコアを 0 でクランプする（下位が 0 に密集し、足切りで切る店の選択が恣意的になる）
- ❌ スコアで客の来やすさを変える（正のフィードバックが二重にかかる）
- ❌ **`matchTimeLimitMs` 相当の「制限時間」パラメータを足す**（デッドラインは `cull.stages` の最終 `atMs` から導く。時間の情報源を2つにしない）
- ❌ 終了条件を `cull.stages` の最終ステージ以外に足す（「生存1店で終了」を復活させない）
- ❌ クライアントに離脱・脱落・順位を報告させる（サーバーが自律確定する）
- ❌ 技術スタック（5章）を勝手に変える
- ❌ プロダクト名・モジュール名を勝手にリネームする（`takoda99` を維持）
- ❌ main へ直接 push する（ブランチ→PR）

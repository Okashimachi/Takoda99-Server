# 本戦 実装計画（plan-honsen）索引

予選通過後、**本戦に向けたサーバー強化**の作業単位ごとの仕様書。
`docs/plan/`（予選までのplan）とは別ディレクトリで、本戦の追加設計をここに集約する。

- **内容の正典は plan**。進捗・担当は GitHub issue（`gh issue list`）。食い違ったら plan が正しい。詳細は `AGENTS.md` §7
- 全体設計は `docs/architecture.md`、責務・禁止事項は `AGENTS.md`
- 予選までの設計は `docs/plan/README.md`

> 本戦の狙い（引き継ぎ資料 Track B/C）：**「プレイヤーのデータを全部残す」を燃料に、観測・Bot・お題・configを賢くする** ＋ **見える基盤にする**。
> ゲーム性の根幹は変えない（2026-08-11 本選方針MTG：単純方向へ舵、ただし proto は不変）。

---

## 前提となった意思決定（2026-08-11 壁打ち）

- 本戦サーバーの主戦場は **①現在情報を見せる観測フロント ②全データDB保存 ③BOT強化 ④アルゴリズム改良**。
- **観測フロントはサーバー同梱**（Go が `/admin` で静的配信、単一デプロイ）。
- **認証は config と同じ共有トークン方式**（`X-Admin-Token` / 定数時間比較）。ブラウザのWS/SSEはヘッダを付けられないので、ストリームのみ `?token=` クエリ or Cookie で配送する。
- 客向け `StoreListUpdate` は #81・MTG方針で**痩せる**。観測フロントは**別系統の管理者ストリーム**に載せ、客向け配信の縮小と独立させる。
- ゲームの決着形式（storm 継続 vs 時間区切り足切り）は **カシューの企画書（8/12 MTG）待ち**。算法改良（h06）は骨子のみ先行し、確定はその後。

---

## plan 一覧（上から着手）

### ★ 実装セッションはまずこれ

| plan | 内容 |
|---|---|
| [h00](plan-h00_共有コントラクト.md) | **観測基盤の共有コントラクト**。実装順序（#104→h01→h02→h03）・`internal/admin` パッケージ・`AdminHub` I/F・配線とライフサイクル・`/admin` サーフェス・ワイヤ契約。h01〜h03 を跨ぐ決定はここに1本化。**実装前に必読** |

### Tier 1 — 観測基盤（企画書に依存しない・今すぐ）

| plan | 内容 | 依存 | issue |
|---|---|---|---|
| [h01](plan-h01_観測ダッシュボードMVP.md) | ライブ観測ダッシュボードMVP。既存 `StoreListUpdate` を購読し99店を体力/評価/順位で俯瞰。**最小改修**（AdminHub＋`/admin/ws`＋静的配信） | なし | #48 |
| [h02](plan-h02_管理者スナップショット配信.md) | 管理者スナップショット配信＋ダッシュボード強化。客分配・フェーズ・heat・storm を含む `AdminSnapshot` を**別系統**で配信。h01を恒久版に置換 | h01 | #48, #81 |

### Tier 2 — データ全量保存（Bot/算法の燃料）

| plan | 内容 | 依存 | issue |
|---|---|---|---|
| [h03](plan-h03_注文単位イベント永続化.md) | `order_attempt` テーブル。注文単位の打鍵/精度/速度をheat別に蓄積。tick中INSERT禁止・試合終了時一括 | PR #104 | #15 |

### Tier 3 — BOT強化

| plan | 内容 | 依存 | issue |
|---|---|---|---|
| [h04](plan-h04_BOTプロファイル生成.md) | `cmd/botprofile`。heat別分位から弱/中/強tierのプロファイルを生成 | h03 | #15 |
| [h05](plan-h05_BOTtier制の反映.md) | `bot_profile` テーブル・DIP注入・tier配分config。DB無時は現行 `BotParams` にフォールバック | h04 | #15 |

### Tier 4 — アルゴリズム改良（★企画書待ち・骨子先行）

| plan | 内容 | 依存 | issue |
|---|---|---|---|
| [h06](plan-h06_バランス改良方針.md) | 課題2/3の整理と改良方針。決着形式の分岐（storm継続 vs 時間区切り足切り）。確定は8/12企画書後 | h02, h03 | — |

---

## 依存関係

```
h01 (MVP・見える) ──→ h02 (管理者ストリーム・恒久化) ──┐
                                                      ├─→ h06 (算法改良・要企画書)
PR#104 (store_result詳細) ──→ h03 (order_attempt) ──┬─┘
                                                    └─→ h04 (プロファイル生成) ──→ h05 (tier反映)
```

**実装順序（確定・詳細は [h00](plan-h00_共有コントラクト.md) §0）**: `#104 マージ → h01 → h02 → h03`。
- #104 は実装セッション開始前にマージしておく（h02/h03 が同ファイルを触るため）。
- h01 は #104 と非依存（順不同可）。h02/h03 は #104 マージ必須。
- **h01/h02/h03 は企画書に依存しない**（今すぐ着手可）。
- **h06 は 8/12 の企画書で決着形式が確定してから**内容を固める。骨子だけ先に置く。

---

## 既存 plan との関係（supersede）

本戦planは自己完結で書き、重複する既存planには冒頭に本戦版への注記を置く（予選での plan-12→plan-17 と同じ運用）。

| 既存 | 本戦版 | 関係 |
|---|---|---|
| `plan-12` / `plan-17`（observability） | **h01 / h02** | 構造化ログの土台を、観測ダッシュボードの管理者スナップショットに統合 |
| `plan-24`（BOT学習ループ） | **h03 / h04 / h05** | Phase2/3/4 を本戦向けに分割・精製 |
